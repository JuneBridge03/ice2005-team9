module controller (
    input clk,          // ?? ?? 
    input rst,          // ?? ??
    output reg write,   // ??? ??(Write) ??? ?? 
    output reg display_en // ????? ??? ?? 
);

    // Clock ??? ?? ??(posedge)?? ?? 
    always @(posedge clk) begin
        if (rst == 1'b1) begin
            // Reset ??? 1? ?? ???? ?? 
            // write ??? display ?? ?? ???? ??? 0?? ??            write <= 1'b0;
            display_en <= 1'b0;
        end
        else begin
            // Reset? 1? ?? ?? ???? ?? ?? 
            // ???? ?? ??? ? ??? write? 1? ?? 
            // ?? ??? ????? display enable ?? ?? 
            write <= 1'b1;
            display_en <= 1'b1;
        end
    end

endmodule